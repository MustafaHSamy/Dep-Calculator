import { StyleSheet, Text, View } from "react-native";
import React from "react";

const UnitsOfProduction = () => {
  return (
    <View>
      <Text>UnitsOfProduction</Text>
    </View>
  );
};

export default UnitsOfProduction;

const styles = StyleSheet.create({});
